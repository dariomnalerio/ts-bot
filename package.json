{
  "name": "ts-bot",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "clean": "del /s /q build",
    "build": "tsc --build",
    "start": "npm run clean && npm run build && node build/index.js",
    "startNoClean": "npm run build && node build/index.js",
    "devStart": "npm run clean && npm run build && node build/index.js --dev",
    "devStartNoClean": "npm run build && node build/index.js --dev",
    "format": "prettier --write ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "discord.js": "^14.15.3",
    "glob": "^10.4.3",
    "mongoose": "^8.5.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "prettier": "^3.3.3"
  }
}